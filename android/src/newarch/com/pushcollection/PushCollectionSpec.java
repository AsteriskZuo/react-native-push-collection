package com.pushcollection;

import com.facebook.react.bridge.ReactApplicationContext;

abstract class PushCollectionSpec extends NativePushCollectionSpec {
  PushCollectionSpec(ReactApplicationContext context) {
    super(context);
  }
}
